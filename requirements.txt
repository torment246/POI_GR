numpy>=1.26,<2
PyYAML>=6.0,<7
scikit-learn>=1.4,<2
sentence-transformers>=5.1,<6
torch>=2.1,<3
tqdm>=4.66,<5
transformers>=4.51,<5
pytest>=8,<9

# Exact GPU retrieval additionally requires Faiss GPU. The project environment
# uses the Conda CUDA build because no compatible wheel is installed by pip:
# conda install -c pytorch -c nvidia faiss-gpu=1.8.0

# 背景

当前地图检索系统采用多阶段检索架构，query -> 召回（多路） -> 排序（MTL）-> 多样性&规则，面临以下问题：

- **复杂query理解不足：**用户输入真实搜索意图包含目标地点、空间范围等，传统搜索难以理解复杂语义

- **长尾POI和新POI召回能力不足：**传统召回主要依赖倒排召回、向量召回、热门召回等，新POI缺少历史行为，低频POI曝光不足，别名、口语化表达匹配困难

- **多阶段架构优化目标割裂：**传统检索中召回阶段优化覆盖率，排序阶段优化点击率，规则阶段优化业务约束，各阶段独立优化，难以整体建模用户，query，空间和POI之间的复杂关系

生成式检索通过大模型生成能力，实现从关键词匹配升级为搜索意图理解；从候选排序升级为直接生成目标POI；统一建模query、用户、空间和POI之间关系。

## 搜不到数据



目前搜不到数据类型分为三大类：

- 数据：数据缺失、数据错误、数据不规范、低热冗余过滤、数据下限和tag缺失。

- 策略：ww召回问题-ww识别错误、限定词泛搜、ww二次召回；纠错-实时搜索纠错、纠错、拼音纠错；省略-输入过程省略、省略、精确门址省略；排序-城市排序问题；分词错误-分词错误；相关性打分问题-打分低被误过滤、被列表洁净模型过滤；缺失召回队列-全拼的周边召回、纠错后的泛搜；向量-向量欠召回。

- 其他：意图不明确、换意图、无法复现、满足意图、非出行意图。

在搜不到数据分析中，生成式检索最优先解决的是**POI在线、用户意图基本明确，但现有关键词、纠错、分词、向量和召回链路没有把它找回来的策略类问题。**

## 现状

**Didi map search：Sug+Search**

地址检索的跟其他场景的本质差异

| 场景     | 视频推荐       | 电商搜索            | 地址 / POI                                |
| -------- | -------------- | ------------------- | ----------------------------------------- |
| 约束     | 类别           | 较强（类别）        | 强（地理空间）                            |
| 文本形态 | 稳定           | 噪声/前缀/口语/错拼 | 噪声 / 别名/ 前缀 / 错拼 （别名问题更多） |
| 候选空间 | 可生成         | 离散 SKU，可索引    | 不可穷举生成                              |
| 实时性   | 秒级           | 毫秒级              | 毫秒级                                    |
| 错误代价 | 低（点错商品） | 低（点错商品）      | 高（导航错误）                            |

- **打车业务（Didi Map Search）与传统的电商推荐相比，最大的区别在于强地理空间约束和意图的高频确定性**



# 目标

构建基于 Semantic ID 的生成式 POI 检索模型，融合用户 Query、POI 语义信息和地理上下文，直接生成 Top-K 合法 POI 候选，并作为现有检索系统的补充召回通道。

首期重点面向目标明确的单 POI 难例搜索，解决错别字、拼音混输、别名省略、地址差异、语义表达不一致及 Where-What 限定等导致的搜不到问题；后续逐步扩展到限定词搜索、泛搜及多 POI 候选召回场景。



# 方案

## 第一版

### POI 特征

POI文本特征主要包括：

- POI名称；

- 详细地址；

- 别名和俗称。

将上述字段拼接为结构化文本，再通过Embedding模型生成POI语义向量。经纬度不与文本直接拼接，而是单独构建地理编码。点击热度、发单热度和layer等动态特征暂不进入SID，可用于后续碰撞展开和POI重排。分类特征用于后续SID分类验证。

### Semantic ID 构建

采用RQ-VAE构建POI语义SID。RQ-VAE对POI Embedding进行残差量化，将连续向量转换为多层离散Token，使语义相近的POI共享部分SID前缀，并通过后续层级逐步表达更细粒度的差异。

SID结构设计为：<GID><RQ_Level1><RQ_Level2><RQ_Level3>

GID优先采用Geohash构建，用于降低名称相似但空间位置不同的POI发生冲突的概率。

RQ-VAE链路跑通后，进一步使用相同Embedding和相同数据训练RQ-KMeans码本，并比较两种方法的SID唯一性、层级语义和下游生成效果。

### SFT 训练

采用监督微调，使模型学习：Query + 位置上下文 → POI SID

后续优化可以混合一定比例的：POI文本 → POI SID 辅助对齐任务。

在SFT模型具备稳定召回能力后，再进行GRPO或DPO等偏好优化。

### 推理与约束解码

推理阶段采用Beam Search生成Top-K SID。

为避免模型生成不存在的编码，将全部有效SID构建为Trie前缀树。模型在每一步只能从当前前缀下实际存在的Token中进行选择，从而保证生成结果能够映射到真实POI。

若一个SID对应多个POI，则将SID展开为POI集合，并结合以下信息进行排序：

- 用户位置与POI距离；

- Query与POI文本相关性；

- POI稳定热度；

- POI质量和有效状态。

最终生成式召回结果与传统召回候选合并后进入统一排序。

### 评测方案

SID层主要评价模型能否生成目标POI对应的编码；POI层则评价SID展开后是否真正召回用户点击或发单的目标POI。

核心指标包括：

- HR@K；

- NDCG@K；

- MRR；

- 有效SID生成率；

- no-result Query命中率；

除全量测试集外，还需要分别观察别名、纠错、省略、数字地址、长Query、长尾POI和新增POI等专项场景。

## 第一版实现前必须明确的事项

以下事项不改变总体技术路线，但必须在对应开发阶段开始前确定：

1. **标签定义：**明确 Query 的目标 POI 来自点击、发单还是人工标注，并定义多目标、无目标和失效 POI 的处理方式。
2. **数据切分：**除随机切分外，单独构造长尾 POI、新 POI 和 no-result 测试集，避免同一 Query 或其近重复表达跨集合泄漏。
3. **GID 粒度：**根据搜索半径和跨区需求选择 Geohash 精度，验证把 GID 放在首 Token 是否会限制跨区域召回。
4. **SID 碰撞：**记录唯一 SID 比例、碰撞组大小和目标 POI 覆盖率，并明确碰撞展开后的排序规则。
5. **Token 设计：**RQ 各层使用有限的专用 Token 集，不为每个 POI 单独增加词表 Token；训练和 Trie 解码使用同一份编码清单。
6. **SFT 约束：**Qwen3-0.6B 使用非思考模式，只对目标 SID 序列计算损失，先完成小样本过拟合测试再运行正式训练。
7. **RL 进入条件：**只有 SFT 的合法 SID 生成率、核心召回指标和约束解码稳定后，才评估 GRPO 或 DPO。

## 最小步骤与核验闸门

每个阶段只完成当前最小闭环，输出代码、测试、运行命令和解释；用户核验通过后再进入下一阶段。

| 阶段 | 本步只做什么 | 核验标准 |
|---|---|---|
| 0. 数据契约 | 确认 POI、Query、标签和切分字段 | 字段、类型、空值和主键规则无歧义 |
| 1. 数据读取 | 实现分块读取、字段校验和合成测试 | 小样本测试通过，真实数据能安全输出汇总 |
| 2. 文本与 Embedding | 确定文本模板，对候选 Embedding 做小样本比较 | 输入模板固定，维度、速度和相似度案例可核验 |
| 3. RQ-VAE SID | 只完成 Embedding 到分层 SID | 重建、码本使用率、唯一性和碰撞指标可复现 |
| 4. GID 与完整编码 | 构建 GID、完整编码和映射 | 编码可逆，目标 POI 覆盖和碰撞展开正确 |
| 5. SFT 数据 | 构建 Query 到编码的训练样本 | 无泄漏，格式、mask 和统计通过测试 |
| 6. SFT smoke test | 用 Qwen3-0.6B 在极小样本上过拟合 | loss 明显下降且能生成合法训练目标 |
| 7. 正式 SFT | 运行受控训练并保存可复现配置 | 验证集指标和实验记录完整 |
| 8. Trie 解码与评测 | 实现约束 Beam Search 和 POI 展开 | 生成编码全部合法，SID/POI 指标可复现 |
| 9. 增量与偏好优化 | 分析 no-result 增量，再决定是否做 RL | 有清晰 SFT 基线和可验证奖励后才进入 |



# Todo

### 1 北京城市链路跑通

- 完成北京POI清洗数据；✅

- 完成Query—目标POI样本和数据切分；

- 完成全量POI向量构建；✅

- 使用RQ-VAE构建语义SID；（旧实现已清理，待按当前方案重新设计和实验）

- 构建Geohash GID和完整POI编码；

- 使用Qwen3-0.6B完成SFT；

- 实现Trie约束Beam Search；

- 完成SID-level和POI-level评测；

- 分析生成式召回对no-result场景的增量。

### 2 方案对比与性能优化

- 使用RQ-KMeans重新构建SID并与RQ-VAE对比；

- 优化POI文本特征和Embedding模型；

- 增加数字Query、别名Query和长尾样本；

- 开展GRPO或DPO偏好优化；

- 评估不同Beam规模下的效果和推理时延。

### 3 多城市与线上化

- 从北京扩展到多城市和全国POI；

- 增加city或area层级Token；

- 加入用户历史行为和Session上下文；

- 建立线上搜索反馈和增量训练机制；

- 开展线上灰度和A/B实验。

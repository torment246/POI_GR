# 仓库工作规则

## 工作定位

- 本仓库是代码开发仓库，不是自动报告生成仓库。
- 默认产出是源码、配置、测试和必要的正式文档更新。
- 优先完成可运行的代码修改，再给出必要说明。

## 开始任务前

- 阅读 `README.md`、`方案.md`、`docs/PROJECT_STATUS.md` 和任务相关实现。
- 先定位并复用现有模块，不重复实现已有功能。
- 不为单次任务随意增加顶层目录。
- 旧实验代码和记录已清除，除非用户明确要求，不要从 Git 历史恢复。

## 方案基线

- `方案.md` 是当前第一版技术方案和实施顺序的唯一基线。
- 实现与方案发生冲突时，先说明冲突、影响和建议，等待用户确认后再改变方向。
- 未经用户确认，不擅自扩大任务范围，不提前实现后续阶段。

## 最小步开发与核验

- 默认一次只完成一个能独立运行、独立测试、独立核验的最小闭环。
- 收到较大需求时，先拆分阶段，只执行当前已确认的第一步；完成后等待用户核验再继续。
- 开始修改前，简要说明本步目标、不包含的内容和验收标准。
- 不为了未来可能使用的功能提前搭建复杂抽象、通用框架或大量占位代码。
- 每一步都应优先包含对应的轻量测试或 smoke test；测试数据使用合成样例，不提交真实业务数据。
- 完成后必须让用户能够回答：改了什么、数据如何流动、关键逻辑在哪里、为什么这样实现、怎样复现验证。
- 用户提出疑问时先解释当前实现，不在解释过程中顺带推进下一阶段。

## 代码可学习性

- 使用清晰的英文变量、函数和模块名；面向用户的文档和命令行说明使用中文。
- 函数保持单一职责，公共或关键接口提供类型标注和简洁英文 docstring。
- 只为设计原因、边界条件和容易误解的行为写注释；明显逻辑不写注释，也不为每个类、函数机械添加 docstring。
- 优先使用直接、可读的实现；只有出现真实复用需求时再抽象。
- 新增命令行脚本时，在最终回复中给出最小命令、输入、输出和预期现象。

## 报告规则

- 默认不为每次任务创建 Markdown、审计、执行过程、迁移或时间戳报告。
- 用户未明确要求报告文件时，只在最终回复中简洁总结。
- 用户明确要求书面报告时，一次任务最多创建一个报告文件。
- 项目状态、实验进展、数据产物和目录变化分别更新 `docs/PROJECT_STATUS.md`、`docs/EXPERIMENT_LOG.md`、`docs/DATA_AND_ARTIFACTS.md`、`docs/REPO_MAP.md`。
- 不为同一主题创建重复 Markdown。
- `docs/` 下的项目文档统一使用中文；第三方模型自带文档不在此限制内。

## 实验记录

- 所有正式实验统一追加到 `docs/EXPERIMENT_LOG.md`，不为单次实验创建新的 Markdown。
- 实验编号使用 `EXP-YYYYMMDD-NN`，同一天从 `01` 递增。
- 每条实验记录必须包含：目标与假设、数据版本、代码提交或工作树状态、配置、命令、环境、核心指标、产物位置、结论和下一步。
- 只记录实际运行结果；未运行、失败或中止的实验必须明确标注，不补写或猜测指标。
- 纯代码整理不伪装成实验；只有影响项目阶段、实验配置或可复现性时才更新实验文档。
- 实验文档不得记录真实业务样本、内部地址、凭证或其他敏感内容。

## 图表与生成文件

- 用户未明确要求时不生成图表，不为展示调试进度生成图片。
- 实验所需临时图表放入 `outputs/figures/`，由 Git 忽略。
- 只有被 README、论文、PPT 或正式文档引用的最终图表才能进入 tracked 目录。
- 不默认生成大量 CSV、JSON、PNG 或 HTML；可由代码重新生成的大型分析文件不得提交 Git。

## 代码开发

- 新脚本必须职责明确，提供 `--help` 和合理错误处理。
- 参数进入配置或命令行，不写死服务器绝对路径。
- 不写入 Token、密码、公司服务器地址或敏感路径。
- 滴滴真实业务数据不得加入公开 Git。
- 模型、checkpoint、Embedding、NPY、Parquet 和实验输出不得加入 Git。
- 新流水线必须先定义真实数据字段契约和轻量测试，再增加训练实现。
- SFT、RL 和完整数据处理必须在前置阶段验收通过后再实现或运行。

## 执行与 Git

- 用户未明确要求时不运行长时间训练；可以运行 compileall、轻量测试、`--help` 和小样本 smoke test。
- 不执行 `git clean`，不删除未知数据或模型。
- 不执行 `git push`，除非用户明确要求。
- 完成前检查 `git diff`、`git status`、compileall 和相关轻量测试。
- 不创建 commit，除非用户明确要求。

## 最终回复

只需说明：

1. 本步修改了什么；
2. 核心实现和数据流；
3. 用户如何验证及验证结果；
4. 当前限制和下一步建议；
5. 是否 commit/push。
